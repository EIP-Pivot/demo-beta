component Mdr = Number

system onTickPhysics(entity<RenderObject>) event Tick(Number delta)
        print("delta", delta)
        print("entity", entity.RenderObject.meshID)
        print("key V", isPressed("V"))
